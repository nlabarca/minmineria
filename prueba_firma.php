<?php
require_once 'vendor/autoload.php';

/*use Firebase\JWT\JWT;
$time = time();
$key = 'my_secret_key';
$token = array(
               'iat' => $time, // Tiempo que inició el token 
               'exp' => $time + (60*60), // Tiempo que expirará el token (+1 hora) 
               'data' => [ // información del usuario 'id' => 1, 
                                'purpose'=> 'Propósito General',
  								'run'=> '15425669',
  								'expiration'=> '2020-05-30T17:31:00',
  								'entity'=> 'Subsecretaría de Minería'
                              ]);
$jwt = JWT::encode($token, $key);//esto es para generar el token
$data = JWT::decode($jwt, $key, array('HS256'));// esto es para decodificar el 

echo $data;*/
?>